# ──────────────────────────────────────────────────────
# NK Browser — зависимости
# ──────────────────────────────────────────────────────
# Установка:
#   pip install -r requirements.txt

# Антидетект браузер (патченный ChromeDriver)
undetected-chromedriver>=3.5.5

# Selenium (подтягивается как зависимость uc, но фиксируем явно)
selenium>=4.15.0

# HTTP-клиент для 2Captcha и диагностики IP
requests>=2.31.0

# ──────────────────────────────────────────────────────
# Остальное — стандартная библиотека Python 3.10+
# (os, json, re, time, random, logging, math, base64,
#  shutil, sqlite3, tempfile, datetime, urllib, secrets)
# Дополнительно устанавливать не нужно.
# ──────────────────────────────────────────────────────
